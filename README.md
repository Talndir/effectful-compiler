A compiler for the lambda calculus written entirely using effects and handlers.

Relies on the *Effective* library, which is currently not public (and not owned by me, so I can't make it public, sorry!). However, the code should be understandable without being able to see the library.